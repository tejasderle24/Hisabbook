<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hisaabbook - Your Digital Ledger</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        
        body {
            font-family: 'Inter', sans-serif;
        }
        
        .nav-gradient {
            background: linear-gradient(135deg, #4F46E5 0%, #7C3AED 100%);
        }
        
        .card-hover {
            transition: all 0.3s ease;
        }
        
        .card-hover:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #4F46E5 0%, #7C3AED 100%);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: linear-gradient(135deg, #4338CA 0%, #6D28D9 100%);
            transform: translateY(-2px);
        }
    </style>
</head>

<body class="bg-gray-50 min-h-screen">
    <nav class="w-full py-5 px-6 flex items-center justify-between text-white nav-gradient shadow-lg">
        <h2 class="text-2xl font-bold flex items-center gap-2">
            <i class="ri-booklet-line"></i>
            Hisaabbook
        </h2>
        <div class="flex gap-6 items-center">
            <a href="/" class="hover:underline flex items-center gap-1">
                <i class="ri-home-4-line"></i>
                Home
            </a>
            <a href="/create" class="bg-white text-indigo-600 px-4 py-2 rounded-md font-medium hover:bg-gray-100 transition flex items-center gap-1">
                <i class="ri-add-circle-line"></i>
                Create New Hisaab
            </a>
        </div>
    </nav>
    
    <main class="max-w-6xl mx-auto px-4 py-8">
        <div class="flex items-center gap-3 mb-8">
            <div class="h-10 w-1 bg-indigo-600 rounded-full"></div>
            <h1 class="text-3xl font-bold text-gray-800">All Hisaab Records</h1>
        </div>
        
        <% if (files.length === 0) { %>
            <div class="bg-white rounded-xl shadow-sm p-8 text-center">
                <i class="ri-file-list-3-line text-5xl text-gray-300 mb-4"></i>
                <h3 class="text-xl font-medium text-gray-600 mb-2">No hisaab records yet</h3>
                <p class="text-gray-500 mb-6">Create your first hisaab to get started</p>
                <a href="/create" class="btn-primary text-white px-6 py-2 rounded-md inline-flex items-center gap-2">
                    <i class="ri-add-line"></i>
                    Create First Hisaab
                </a>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                <% files.forEach((elem)=>{ %>
                    <div class="bg-white rounded-xl shadow-sm p-5 card-hover border border-gray-100">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center gap-3">
                                <div class="h-10 w-10 bg-indigo-100 rounded-full flex items-center justify-center">
                                    <i class="ri-file-text-line text-indigo-600"></i>
                                </div>
                                <h2 class="font-medium text-gray-800 truncate max-w-xs">
                                    <%= elem.replace('.txt', '') %>
                                </h2>
                            </div>
                            <div class="flex gap-3">
                                <a href="/edit/<%= elem%>" class="h-9 w-9 bg-blue-100 rounded-full flex items-center justify-center hover:bg-blue-200 transition" title="Edit">
                                    <i class="ri-edit-line text-blue-600"></i>
                                </a>
                                <a href="/delete/<%= elem %>" class="h-9 w-9 bg-red-100 rounded-full flex items-center justify-center hover:bg-red-200 transition" title="Delete" onclick="return confirm('Are you sure you want to delete this hisaab?')">
                                    <i class="ri-delete-bin-line text-red-600"></i>
                                </a>
                            </div>
                        </div>
                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-between items-center">
                            <span class="text-sm text-gray-500">
                                <%= elem.split('.')[0] %>
                            </span>
                            <a href="/edit/<%= elem%>" class="text-sm text-indigo-600 hover:underline flex items-center gap-1">
                                View Details
                                <i class="ri-arrow-right-line"></i>
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </main>
    
    <footer class="mt-12 py-6 text-center text-gray-500 text-sm border-t border-gray-200">
        <p>© 2023 Hisaabbook. Your digital ledger for managing accounts.</p>
    </footer>
</body>

</html>